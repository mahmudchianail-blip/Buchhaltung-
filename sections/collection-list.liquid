<style>
/* ===== MODERNE COLLECTION-LIST – nur CSS, kein HTML geändert ===== */
/* wirkt nur in dieser Section via ID-Scoped-Selektoren */
.section-{{ section.id }}-padding{
  --cl-radius: 22px;
  --cl-shadow: 0 18px 40px rgba(0,0,0,.08);
  --cl-shadow-hover: 0 24px 54px rgba(0,0,0,.12);
  --cl-border: rgba(0,0,0,.08);
  --cl-ink: #111;
  --cl-ink-dim: #444;
}

/* Section-Headline mit Akzent-Linie */
.section-collection-list .collection-list-title{
  position: relative;
  letter-spacing: .01em;
}
.section-collection-list .collection-list-title::after{
  content:"";
  display:block;
  width: 56px; height: 3px;
  margin-top: .6rem;
  border-radius: 3px;
  background: currentColor;
  opacity: .18;
}

/* Grid-Abstände etwas luftiger */
.section-collection-list .collection-list{
  gap: clamp(18px, 3vw, 28px);
}

/* Card: runde Ecken, Shadow, feiner Rand */
.section-collection-list .collection-list .card{
  border-radius: var(--cl-radius);
  overflow: hidden;
  background: #fff;
  box-shadow: var(--cl-shadow);
  border: 1px solid var(--cl-border);
  transition: transform .25s ease, box-shadow .25s ease, border-color .25s ease;
}

/* Media: sanfter Zoom + Gradient oben/unten für bessere Lesbarkeit */
.section-collection-list .collection-list .card__media{
  position: relative;
  overflow: hidden;
}
.section-collection-list .collection-list .card__media::after{
  content:"";
  position:absolute; inset:0;
  background:
    linear-gradient(to bottom, rgba(0,0,0,.10), rgba(0,0,0,0) 30%),
    linear-gradient(to top, rgba(0,0,0,.08), rgba(0,0,0,0) 35%);
  pointer-events:none;
  transition: opacity .25s ease;
  opacity: .5;
}
.section-collection-list .collection-list .card__media img{
  transform: scale(1.02);
  transition: transform .6s cubic-bezier(.22,.61,.36,1);
  will-change: transform;
}

/* Info-Bereich */
.section-collection-list .collection-list .card__information{
  padding: 18px 18px 20px 18px;
}
.section-collection-list .collection-list .card__heading{
  margin: 0;
  font-weight: 700;
  color: var(--cl-ink);
  letter-spacing: .02em;
  line-height: 1.15;
}
.section-collection-list .collection-list .card__heading .full-unstyled-link{
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: .55rem;
  text-decoration: none;
  outline: none;
}
.section-collection-list .collection-list .card__heading .full-unstyled-link::after{
  content: "→";
  font-weight: 600;
  transform: translateX(0);
  transition: transform .25s ease, opacity .25s ease;
  opacity: .75;
}

/* Hover/Fokus-Effekte – Card hebt sich, Bild zoomt dezent */
.section-collection-list .collection-list .card:hover,
.section-collection-list .collection-list .card:focus-within{
  transform: translateY(-6px);
  box-shadow: var(--cl-shadow-hover);
  border-color: rgba(0,0,0,.12);
}
.section-collection-list .collection-list .card:hover .card__media img,
.section-collection-list .collection-list .card:focus-within .card__media img{
  transform: scale(1.06);
}
.section-collection-list .collection-list .card:hover .card__media::after,
.section-collection-list .collection-list .card:focus-within .card__media::after{
  opacity: .65;
}
.section-collection-list .collection-list .card:hover .full-unstyled-link::after,
.section-collection-list .collection-list .card:focus-within .full-unstyled-link::after{
  transform: translateX(4px);
  opacity: 1;
}

/* Tastatur-Fokus sichtbar & schön */
.section-collection-list .collection-list .card:focus-within{
  outline: 3px solid rgba(0,115,230,.25);
  outline-offset: 2px;
}

/* Slider-Buttons (falls Mobile-Swipe aktiv) schicker & runder */
.section-collection-list .slider-buttons{
  gap: .25rem;
}
.section-collection-list .slider-button{
  width: 44px; height: 44px;
  border-radius: 999px;
  border: 1px solid var(--cl-border);
  background: #fff;
  box-shadow: 0 8px 18px rgba(0,0,0,.08);
  transition: transform .15s ease, background .2s ease, border-color .2s ease;
}
.section-collection-list .slider-button:hover{
  transform: translateY(-2px);
  border-color: rgba(0,0,0,.16);
}

/* Responsive Feinschliff */
@media (max-width: 749px){
  .section-collection-list .collection-list{
    gap: 14px;
  }
  .section-collection-list .collection-list .card__information{
    padding: 14px 14px 16px 14px;
  }
}
</style>
